# Legacy runtime notice

This directory is retained for provenance and historical comparison.

The active repository runtime and public semantic surface are under:

- `origins/`
- `origins.abiogenesis`
- `scripts/run_abiogenesis.py`

`src/origin_of_life/` contains an older parallel implementation with stale
scenario parameters and must not be treated as an equal-status runtime.

This notice is additive. Historical files are intentionally preserved.
